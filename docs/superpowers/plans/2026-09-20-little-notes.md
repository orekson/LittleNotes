# 小小筆記 1.1

依使用者已確認的需求直接實作：保留套件 ID、SQLite 與簽章，升級版本 2 / 1.1。

1. 將文字格式列移到本文之後、貼圖／預覽之前；統一產品名稱。
2. 擴充既有富文字 JSON：圖片種類與尺寸、具穩定 ID 的勾選方框；舊格式可讀。
3. 圖片插入與再次調整尺寸，滑桿與雙指縮放；維持比例並限制於可用寬度。
4. 完整 31 張同來源貼圖，捲動選單。
5. 小工具 bitmap 分片配合原生點擊區域，明確 PendingIntent 廣播切換方框；資料庫交易避免競爭。
6. 驗證格式往返、大小上限、方框狀態持久化與編輯衝突合併、實際桌面點击；回歸既有測試。
7. 整理 README、更新流程、CI、版本紀錄，初始化 Git 並上傳使用者 GitHub。

GitHub 可見性已詢問；尚未答覆時預設私人，第三方素材來源另列。

## 執行紀錄

程式、31 張素材、簽章相容、5 項單元與 15 項装置測試、真實 Launcher 勾選／取消、圖片縮放已完成。独立審查與實測發現的換行點擊、方向寬度、巢狀 RemoteViews 點擊限制均已修正。CI 與維護文件已建立；GitHub 建立儲存庫仍需使用者完成瀏覽器登入，既有本機 Git 憑證已失效。
